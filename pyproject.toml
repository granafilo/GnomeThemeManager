[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gnome-theme-manager"
dynamic = ["version"]
description = "Manager modulare in Python per temi GTK, icone e cursori su ambiente desktop GNOME."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "GPL-3.0-or-later" }
authors = [
    { name = "GnomeThemeManager Contributors" }
]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Environment :: X11 Applications :: Gnome",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Desktop Environment :: Gnome",
]
dependencies = [
    "PyGObject>=3.42.0",
    "requests>=2.28.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.1.0",
]

[project.scripts]
gnome-theme-manager = "gnome_theme_manager.cli.main:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "gnome_theme_manager.__version__"}

[tool.setuptools.package-data]
"gnome_theme_manager" = ["locale/**/*", "locale/*/LC_MESSAGES/*.mo"]
"gnome_theme_manager.gui_gtk" = ["ui/*.ui"]

[tool.pytest.ini_options]
minversion = "7.0"
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra -q"

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
ignore = ["BLE001", "SIM117", "S110"]

[tool.mypy]
python_version = "3.10"
strict = true
warn_unused_configs = true
show_error_codes = true

[[tool.mypy.overrides]]
module = [
    "gi",
    "gi.*",
    "gi.repository.*",
]
ignore_missing_imports = true
follow_imports = "silent"

[[tool.mypy.overrides]]
module = "gnome_theme_manager.gui_gtk.*"
disallow_subclassing_any = false
disallow_untyped_calls = false
disallow_untyped_decorators = false
warn_return_any = false


